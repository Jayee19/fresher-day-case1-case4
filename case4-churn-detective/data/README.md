Place the official pack file here as `case4_telecom_churn.csv`, or generate a stand-in dataset:

```bash
cd ../scripts && python generate_synthetic_churn.py
```

That script writes `../data/case4_telecom_churn.csv` with the same column names and roughly 7k rows so the notebook runs before you have the course CSV.
